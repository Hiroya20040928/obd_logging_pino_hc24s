name: Build Pino Auto Logger v3.2 unsigned IPA

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 26
        shell: bash
        run: |
          set -euo pipefail
          if [ -d /Applications/Xcode_26.5.app ]; then
            sudo xcode-select -s /Applications/Xcode_26.5.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.4.1.app ]; then
            sudo xcode-select -s /Applications/Xcode_26.4.1.app/Contents/Developer
          else
            sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          fi
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-version

      - name: Run protocol parser tests
        shell: bash
        run: |
          set -euo pipefail
          cd CoreValidation
          swift test

      - name: Build device app without code signing
        shell: bash
        run: |
          set -euo pipefail
          rm -rf build Payload
          xcodebuild \
            -project PinoAutoLogger.xcodeproj \
            -target PinoAutoLogger \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            CONFIGURATION_BUILD_DIR="$PWD/build" \
            build
          test -d build/PinoAutoLogger.app

      - name: Verify app bundle
        shell: bash
        run: |
          set -euo pipefail
          /usr/libexec/PlistBuddy -c 'Print :CFBundleExecutable' build/PinoAutoLogger.app/Info.plist
          /usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' build/PinoAutoLogger.app/Info.plist
          /usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' build/PinoAutoLogger.app/Info.plist
          test -x build/PinoAutoLogger.app/PinoAutoLogger

      - name: Package unsigned IPA
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p Payload
          cp -R build/PinoAutoLogger.app Payload/
          /usr/bin/zip -qry PinoAutoLogger-HC24S-v3.2-unsigned.ipa Payload
          shasum -a 256 PinoAutoLogger-HC24S-v3.2-unsigned.ipa | tee PinoAutoLogger-HC24S-v3.2-unsigned.sha256

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: PinoAutoLogger-HC24S-v3.2-unsigned-IPA
          path: |
            PinoAutoLogger-HC24S-v3.2-unsigned.ipa
            PinoAutoLogger-HC24S-v3.2-unsigned.sha256
          retention-days: 30
